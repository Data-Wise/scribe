---
description: Run the Scribe app in development mode
---

# Run Development Server

// turbo-all

1. Rebuild native modules (only needed after npm install):

```bash
npx electron-rebuild
```

1. Start development server:

```bash
npm run dev
```

The app will open automatically. Hot reload is enabled.

## Keyboard Shortcuts

| Action | Shortcut |
|--------|----------|
| Focus mode | ⌘⇧F |
| Exit focus | Escape |
| Wiki link | `[[` then select |
| Tag | `#` then select |
